The quizjet authoring has two components.

The first is "QuizJetAuthoring-master", which is the backend developed using java.

The second is "QuizJetAuthoring-UI-React", which is the UI developed using React js.
